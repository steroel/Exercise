#ifndef __TypeDefinitions_HPP__
#define __TypeDefinitions_HPP__

#include <stdint.h>
//#include <string>

#include "../../Hardware_Abstraction/Device_Driver_Abstraction/STM32F7xx/NVIC_Dispatcher/mSTM32F7xxNVIC_Types.hpp"

using vsint8_t = volatile char;
using vuint8_t = volatile unsigned char;

using vsint16_t = volatile short;
using vuint16_t = volatile unsigned short;

using vsint32_t = volatile int;
using vuint32_t = volatile unsigned int;

using vsint64_t = volatile long;
using vuint64_t = volatile unsigned long;


#endif // __TypeDefinitions_HPP__
